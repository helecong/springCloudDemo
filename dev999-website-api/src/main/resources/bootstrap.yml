spring:
  application:
    name: website-api
  content-path: /v1
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
#      password: ${CONFIG_SERVICE_PASSWORD}
#      username: user
